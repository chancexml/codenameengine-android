name: Mobile Build
run-name: ${{ github.event.inputs.build_name }}

on:
  #push:
    #branches: [main]
  workflow_dispatch:
    inputs:
      build_name:
        description: Build Name
        required: true
        default: Mobile Build
      clean:
        description: Use Lime Clean?
        type: boolean
        required: true
        default: true
      android_flags:
        description: Android Flags
        required: false
        default:
      rebuild_cache:
        description: "Rebuild Cache?"
        required: true
        default: "No"
        type: choice
        options:
          - "Yes"
          - "No"

jobs:
  buildAndroid:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.6

      - name: Restore cache
        if: ${{ github.event.inputs.rebuild_cache != 'Yes' }}
        uses: actions/cache/restore@v4
        with:
         path: |
          ~/.haxelib
          ~/.lime
          ~/.openfl
          ~/.gradle/wrapper
          ~/Android/Sdk
          /home/runner/hxcpp_cache
         key: codename-cache-${{ runner.os }}-${{ hashFiles('**/project.xml') }}
         restore-keys: |
          codename-cache-${{ runner.os }}-

      - name: Setup hxcpp cache
        run: |
         mkdir -p /home/runner/hxcpp_cache
         echo '<xml>
          <set name="HXCPP_COMPILE_CACHE" value="/home/runner/hxcpp_cache" />
          <set name="HXCPP_CACHE_MB" value="4096" />
         </xml>' > ~/.hxcpp_config.xml

      - name: Setup environment
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang openjdk-17-jdk wget unzip

          haxelib setup ~/.haxelib

          haxe -cp commandline -D analyzer-optimize --run Main setup -s
          
          printf "%s\n%s\n%s\n" \
            "$ANDROID_HOME" \
            "$ANDROID_NDK_ROOT" \
            "$JAVA_HOME" \
            | haxelib run lime setup android  
          
          haxelib list

      - name: Install Android SDK + NDK
        run: |
          mkdir -p ~/android-sdk
          cd ~/android-sdk
          wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip commandlinetools-linux-*.zip

          mkdir cmdline-tools/latest
          mv cmdline-tools/* cmdline-tools/latest/ || true

          yes | ~/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses
          ~/android-sdk/cmdline-tools/latest/bin/sdkmanager \
            "platform-tools" \
            "platforms;android-33" \
            "build-tools;33.0.2" \
            "ndk;29.0.14206865"

          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$HOME/android-sdk/ndk/29.0.14206865" >> $GITHUB_ENV

      - name: Clean Android
        if: ${{ github.event_name == 'push' || github.event.inputs.clean == 'true' }}
        run: haxelib run lime clean android

      - name: Install Android-Tools
        run: |
         haxelib remove extension-androidtools
         haxelib git extension-androidtools https://github.com/ChanceXML/androidTools.git
         #haxelib git extension-androidtools https://github.com/ChanceXML/extension-androidtools.git

      - name: Configure Gradle (5GB Safe Mode)
        run: |
          mkdir -p ~/.gradle
          echo "org.gradle.jvmargs=-Xmx5g -XX:MaxMetaspaceSize=1536m -Dfile.encoding=UTF-8" > ~/.gradle/gradle.properties
          echo "org.gradle.daemon=false" >> ~/.gradle/gradle.properties
          echo "org.gradle.parallel=false" >> ~/.gradle/gradle.properties
          echo "org.gradle.configureondemand=false" >> ~/.gradle/gradle.properties
          echo "android.splits.abi.enable=false" >> ~/.gradle/gradle.properties
          
      - name: Limit Java heap globally
        run: echo "JAVA_TOOL_OPTIONS=-Xmx5g" >> $GITHUB_ENV
        
      - name: Build APK
        run: |
         haxelib run lime build android -clean ${{ github.event.inputs.android_flags }}

      - name: Save cache
        if: ${{ always() && github.event.inputs.rebuild_cache != 'Yes' }}
        uses: actions/cache/save@v4
        with:
         path: |
          ~/.haxelib
          ~/.lime
          ~/.openfl
          ~/.gradle/wrapper
          ~/Android/Sdk
          /home/runner/hxcpp_cache
         key: codename-cache-${{ runner.os }}-${{ hashFiles('**/project.xml') }}

      - name: Find APK
        run: find . -name "*.apk"
        
      - uses: actions/upload-artifact@v4
        with:
          name: Android-Build
          path: export/release/android/bin/app/build/outputs/apk/debug/*.apk
